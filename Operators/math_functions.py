x = 2.9
x1 = -2.9
print(round(x)) # Rounds the number to whole number
print(abs(x1)) # Returns the positive representation of a number
# To use math functions, you need to import a math module which contains a bunch of reusable math functions
import math
print(math.ceil(2.9))
print(math.floor(2.9))
# To learn about these math modules on your own, search for python 3 math modules
